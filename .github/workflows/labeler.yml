name: Label PRs

on:
  pull_request:

permissions:
  contents: read
  pull-requests: write

jobs:
  label:
    name: Auto-label by changed files
    runs-on: ubuntu-latest
    steps:
      - uses: actions/labeler@v5
